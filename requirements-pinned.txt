# =============================================================================
# AgentOps Platform — PINNED Versions (POC Spike Validated)
#
# These exact versions were validated during POC Spike (Phase 0).
# DO NOT upgrade without re-running POC spike tests.
#
# Known breaking changes:
#   - mcp >= 1.26.0: FastMCP() no longer accepts 'description' kwarg;
#     port must be set in constructor FastMCP("name", port=8000),
#     NOT in mcp.run(transport="sse", port=8000)
#   - langchain-mcp-adapters >= 0.2.1: MultiServerMCPClient is NOT an async
#     context manager — use `client = MultiServerMCPClient(...)` directly,
#     then `tools = await client.get_tools()`
#   - langgraph-prebuilt >= 1.0.8: create_react_agent shows deprecation
#     warning (suppress with warnings.filterwarnings)
#
# Generated: 2026-03-09
# =============================================================================

# --- Web framework ---
fastapi==0.135.1
uvicorn==0.41.0
pydantic==2.12.5

# --- Google Gemini ---
google-genai==1.66.0

# --- LangChain / LangGraph ---
langchain==1.2.10
langchain-core==1.2.17
langchain-google-genai==4.2.1
langgraph==1.0.10
langgraph-prebuilt==1.0.8
langgraph-checkpoint==4.0.1
langgraph-sdk==0.3.9

# --- MCP ---
mcp==1.26.0
langchain-mcp-adapters==0.2.1

# --- AWS SDK ---
boto3==1.42.63

# --- Utilities ---
python-dotenv==1.2.2
httpx==0.28.1
httpx-sse==0.4.3
